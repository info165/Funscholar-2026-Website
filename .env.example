# Copy to .env.local for development, and set the same values in your host's
# dashboard for production.

# Where contact form submissions are emailed. Get a key at web3forms.com —
# it is free and only asks you to verify the destination address.
# Without this the form returns an error and nothing is sent.
WEB3FORMS_ACCESS_KEY=
